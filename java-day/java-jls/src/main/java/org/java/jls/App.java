package org.java.jls;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
    	
		/*
		 * The Java programming language is strongly and statically typed. This
		 * specification clearly distinguishes between the compile-time errors
		 * that can and must be detected at compile time, and those that occur
		 * at run time. Compile time normally consists of translating programs
		 * into a machine-independent byte code representation. Run-time
		 * activities include loading and linking of the classes needed to
		 * execute a program, optional machine code generation and dynamic
		 * optimization of the program, and actual program execution.
		 */
    	
    	// primitive types
    	
    	//Reference types
		/**
		 * class types, the interface types, and the array types All objects
		 * (including arrays) support the methods of the class Object, which is
		 * the (single) root of the class hierarchy 
		 * (拆箱，装箱)wrapping and unwrapping is
		 * performed automatically by the compiler (in which case, wrapping is
		 * called boxing, and unwrapping is called unboxing)
		 */
    	
    	int[]  arrays = new int[]{3,7};
    	
    }
}
